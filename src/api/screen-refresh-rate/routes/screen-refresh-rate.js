'use strict';

/**
 * screen-refresh-rate router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::screen-refresh-rate.screen-refresh-rate');
