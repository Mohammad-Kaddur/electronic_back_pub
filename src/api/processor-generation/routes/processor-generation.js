'use strict';

/**
 * processor-generation router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::processor-generation.processor-generation');
